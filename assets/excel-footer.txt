WORKSHEET_FOOTER (CONTENT)
    AS (
        SELECT '</Table></Worksheet>'
        FROM SYSIBM.SYSDUMMY1
)
SELECT CONTENT
    FROM WORKSHEET_VOKP21_STURC_BODY
UNION ALL
SELECT * 
    FROM SELECT_MUSTER
UNION ALL
SELECT *
    FROM SELECT_KEYS